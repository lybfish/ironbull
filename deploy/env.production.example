# ============================================================
# IronBull 生产环境变量配置（复制为 .env.production 后填入实际值）
# 优先级：环境变量 > config/default.yaml > 代码默认值
# 所有 IRONBULL_ 前缀会自动映射为配置项（去掉前缀后小写）
# ============================================================

# ---- 环境标识 ----
IRONBULL_ENV=production

# ---- JWT（必须！禁止使用默认值）----
IRONBULL_JWT_SECRET=<替换为 64 位随机字符串: openssl rand -hex 32>

# ---- 数据库 ----
IRONBULL_DB_HOST=127.0.0.1
IRONBULL_DB_PORT=3306
IRONBULL_DB_USER=ironbull
IRONBULL_DB_PASSWORD=<数据库密码>
IRONBULL_DB_NAME=ironbull

# ---- Redis ----
IRONBULL_REDIS_HOST=127.0.0.1
IRONBULL_REDIS_PORT=6379
IRONBULL_REDIS_PASSWORD=<Redis 密码>

# ---- CORS（逗号分隔，生产域名）----
IRONBULL_CORS_ORIGINS=https://admin.ironbull.example.com

# ---- 执行节点鉴权 ----
IRONBULL_NODE_AUTH_SECRET=<替换为随机字符串: openssl rand -hex 16>

# ---- 日志 ----
IRONBULL_LOG_LEVEL=INFO
IRONBULL_LOG_STRUCTURED=true

# ---- 交易所（如需自动交易）----
# IRONBULL_EXCHANGE_API_KEY=
# IRONBULL_EXCHANGE_API_SECRET=
# IRONBULL_EXCHANGE_SANDBOX=false

# ---- Telegram 通知 ----
# IRONBULL_TELEGRAM_BOT_TOKEN=
# IRONBULL_TELEGRAM_CHAT_ID=
